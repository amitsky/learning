specVersion: "v1"
serviceId: "learning"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "learning-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
